export function fromCallback(callback) {
  return function stream(fn) {

  }
}

export function reduce(stream, reducer) {

}

export function write(stream, value) {

}

export function listen(stream, fn) {
  stream(fn)
}
